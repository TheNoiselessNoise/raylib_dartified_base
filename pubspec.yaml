name: raylib_dartified_base
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
publish_to: none

environment:
  sdk: ^3.10.7

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  meta: ^1.18.2

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
